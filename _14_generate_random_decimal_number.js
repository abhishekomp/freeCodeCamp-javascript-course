function generateRandomDecimal(){
  // this will return a decimal less than 1. It can be zero as well.
  // Returns a decimal number between 0 and 1 (0 inclusive, excluding 1)
  return Math.random();
}

console.log(generateRandomDecimal());